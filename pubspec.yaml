name: elkhalfy
description: ELKHALFY IPTV - مشغل IPTV الاحترافي
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  video_player: ^2.9.2
  shared_preferences: ^2.3.2
  cached_network_image: ^3.4.1
  provider: ^6.1.2
  wakelock_plus: ^1.2.10

dependency_overrides:
  package_info_plus: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
